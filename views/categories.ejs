<h1 class="pb-4">Explore by Platforms</h1>

<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/">Home</a></li>
    <li class="breadcrumb-item"><a href="/categories">Platforms</a></li>
    <li class="breadcrumb-item active" aria-current="page">Explore By Platform</li>
  </ol>
</nav>

<div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3 mb-4">
  <% if (Array.isArray(categories) && categories.length > 0) { %>
    <% categories.forEach(function(category){ %>
      <a href="/categories/<%- category.name %>" class="col text-center category__link">
        <div class="category__img shadow category__img--large">
          <img src="/img/<%- category.image %>" alt="<%- category.name %>" loading="lazy">
        </div>
        <div class="pt-1"><%- category.name %></div>
      </a>
    <% }) %>
  <% } %>
</div>

<% if (Array.isArray(categoryById)) { %>
  <h2 class="h4 mb-3">Games in <%= selectedCategory || 'selected category' %></h2>
  <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
    <% if (categoryById.length > 0) { %>
      <% categoryById.forEach(function(game){ %>
        <a href="/game/<%= game._id %>" class="col text-center category__link">
          <div class="category__img category__img--large shadow">
            <img src="/uploads/<%= game.image %>" alt="<%= game.name %>" loading="lazy">
          </div>
          <div class="pt-1"><%= game.name %></div>
        </a>
      <% }) %>
    <% } else { %>
      <p>No items found.</p>
    <% } %>
  </div>
<% } %>
